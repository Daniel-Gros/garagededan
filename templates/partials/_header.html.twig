<header>
	<div class="row">
		<nav class="navbar navbar-expand-md navbar-light bg-light ">
			<a href="{{ path('app_home') }}">
				<img class="logo" src="{{ asset('build/img/various/logo.png') }}" alt="logo du garage" height="40">
			</a>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse justify-content-end" id="navbarNav">
				<ul class="navbar-nav ml-auto gap-3">
					<li class="nav-item">
						<a class="nav-link text-secondary" href="{{ path('app_home') }}">Accueil</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-secondary" href="{{ path('app_showcar')}}">Véhicules</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-secondary" href="{{ path('app_showservice')}}">Services</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-secondary" href="#">Contact</a>
					</li>
					{% if is_granted('ROLE_ADMIN') %}
						<li class="nav-item">
							<a class="nav-link text-secondary" href="{{ path('admin') }}">Admin</a>
						</li>
					{% endif %}
					{# {% if is_granted('ROLE_USER') %}
						<li class="nav-item">
							<a class="nav-link text-secondary" href="{{ path('employee') }}">Admin</a>
						</li>
					{% endif %} #}
					<li class="nav-item">
						<a class="btn bg-dark text-light me-2" href="{{ path('app_login') }}">Connexion</a>
					</li>
					<li class="nav-item">
						<a class="btn bg-dark text-light" href="{{ path('app_logout') }}">Déconnexion</a>
					</li>
				</ul>
			</div>
		</nav>
	</div>
</header>
